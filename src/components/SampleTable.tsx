import * as React from 'react';

export const SampleTable = () => {
  return <div className='table'>
    <span>Update the following code as you need and break them into smaller component as you see fit</span>
  </div>
}